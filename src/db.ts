import Database from "better-sqlite3";
import { mkdirSync } from "node:fs";
import { dirname } from "node:path";
import { randomUUID } from "node:crypto";
import { config } from "./config.js";
import { applyMigrations } from "./migrations.js";

mkdirSync(dirname(config.DATABASE_PATH),{recursive:true});export const db=new Database(config.DATABASE_PATH);db.pragma("journal_mode = WAL");db.pragma("foreign_keys = ON");applyMigrations(db);
export type RunRow={id:string;project_id:string;cycle_id:string|null;prompt:string;provider:string;model:string;status:string;output:string|null;created_at:string;completed_at:string|null};
export function getSchemaVersion():number{return(db.prepare(`SELECT COALESCE(MAX(version),0) AS version FROM schema_migrations`).get() as {version:number}).version;}
export function listSchemaMigrations(){return db.prepare(`SELECT version,name,applied_at FROM schema_migrations ORDER BY version ASC`).all();}
export function createProject(name:string,description:string){const id=randomUUID();db.prepare(`INSERT INTO projects(id,name,description,created_at) VALUES(?,?,?,?)`).run(id,name,description,new Date().toISOString());return getProject(id);}
export function getProject(id:string){return db.prepare(`SELECT * FROM projects WHERE id=?`).get(id);}
export function listProjects(){return db.prepare(`SELECT * FROM projects ORDER BY created_at DESC`).all();}
export function createFactoryCycle(projectId:string,objective:string,constraints:string[]){const id=randomUUID();db.prepare(`INSERT INTO factory_cycles(id,project_id,objective,constraints_json,status,created_at) VALUES(?,?,?,?,?,?)`).run(id,projectId,objective,JSON.stringify(constraints),"running",new Date().toISOString());return id;}
export function getFactoryCycle(id:string){return db.prepare(`SELECT * FROM factory_cycles WHERE id=?`).get(id);}
export function updateFactoryCycleObjective(id:string,objective:string){db.prepare(`UPDATE factory_cycles SET objective=? WHERE id=?`).run(objective,id);}
export function listFactoryCycles(projectId?:string){return projectId?db.prepare(`SELECT * FROM factory_cycles WHERE project_id=? ORDER BY created_at DESC`).all(projectId):db.prepare(`SELECT * FROM factory_cycles ORDER BY created_at DESC`).all();}
export function finishFactoryCycle(id:string,status:string){db.prepare(`UPDATE factory_cycles SET status=?,completed_at=? WHERE id=?`).run(status,new Date().toISOString(),id);}
export function createRun(projectId:string,prompt:string,provider:string,model:string,cycleId?:string){const id=randomUUID();db.prepare(`INSERT INTO runs(id,project_id,cycle_id,prompt,provider,model,status,created_at) VALUES(?,?,?,?,?,?,?,?)`).run(id,projectId,cycleId??null,prompt,provider,model,"running",new Date().toISOString());return id;}
export function finishRun(id:string,status:string,output:string){db.prepare(`UPDATE runs SET status=?,output=?,completed_at=? WHERE id=?`).run(status,output,new Date().toISOString(),id);}
export function addEvent(runId:string,type:string,payload:unknown,cycleId?:string){const resolvedCycleId=cycleId??((db.prepare(`SELECT cycle_id FROM runs WHERE id=?`).get(runId) as {cycle_id:string|null}|undefined)?.cycle_id??null);db.prepare(`INSERT INTO events(id,run_id,cycle_id,type,payload,created_at) VALUES(?,?,?,?,?,?)`).run(randomUUID(),runId,resolvedCycleId, type,JSON.stringify(payload),new Date().toISOString());}
export function addCycleEvent(cycleId:string,type:string,payload:unknown){db.prepare(`INSERT INTO events(id,run_id,cycle_id,type,payload,created_at) VALUES(?,?,?,?,?,?)`).run(randomUUID(),`cycle:${cycleId}`,cycleId,type,JSON.stringify(payload),new Date().toISOString());}
export function listEvents(runId:string){return db.prepare(`SELECT id,run_id,cycle_id,type,payload,created_at FROM events WHERE run_id=? ORDER BY created_at ASC`).all(runId);}
export function listCycleEvents(cycleId:string){return db.prepare(`SELECT id,run_id,cycle_id,type,payload,created_at FROM events WHERE cycle_id=? ORDER BY created_at ASC`).all(cycleId);}
export function getRun(id:string):RunRow|undefined{return db.prepare(`SELECT * FROM runs WHERE id=?`).get(id) as RunRow|undefined;}
export function createRequirement(projectId:string,source:string,key:string,value:unknown,unit?:string|null,required=true){const id=randomUUID(),timestamp=new Date().toISOString(),serializedValue=typeof value==="string"?value:JSON.stringify(value);const requirementColumns=new Set((db.prepare(`PRAGMA table_info(requirements)`).all() as Array<{name:string}>).map(column=>column.name));const priority=requirementColumns.has("priority")?"should":undefined;const verificationStatus=requirementColumns.has("verification_status")?"unverified":undefined;if(requirementColumns.has("description")){if(requirementColumns.has("category")){if(requirementColumns.has("priority")){if(requirementColumns.has("verification_status")){db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,category,priority,verification_status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,"other",priority,verificationStatus,timestamp,timestamp);}else{db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,category,priority,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,"other",priority,timestamp,timestamp);}}else if(requirementColumns.has("verification_status")){db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,category,verification_status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,"other",verificationStatus,timestamp,timestamp);}else{db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,category,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,"other",timestamp,timestamp);}}else if(requirementColumns.has("priority")){if(requirementColumns.has("verification_status")){db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,priority,verification_status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,priority,verificationStatus,timestamp,timestamp);}else{db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,priority,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,priority,timestamp,timestamp);}}else if(requirementColumns.has("verification_status")){db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,description,verification_status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",`${key}: ${serializedValue}`,"other",verificationStatus,timestamp,timestamp);}else{db.prepare(`INSERT INTO requirements(id,project_id,source,key,value,unit,required,status,created_at,updated_at) VALUES(?,?,?,?,?,?,?,?,?,?)`).run(id,projectId,source,key,serializedValue,unit??null,required?1:0,"active",timestamp,timestamp);}}return id;}
export function getRequirement(id:string){return db.prepare(`SELECT * FROM requirements WHERE id=?`).get(id);}
export function listRequirements(projectId:string){return db.prepare(`SELECT * FROM requirements WHERE project_id=? ORDER BY created_at ASC`).all(projectId);}
export function updateRequirementStatus(id:string,status:string){db.prepare(`UPDATE requirements SET status=?,updated_at=? WHERE id=?`).run(status,new Date().toISOString(),id);}
export function createArtifact(projectId:string,runId:string|undefined,kind:string,name:string,uri?:string|null,contentHash?:string|null,metadata?:unknown){const id=randomUUID(),now=new Date().toISOString(),serialized=JSON.stringify(metadata??{});const tx=db.transaction(()=>{db.prepare(`INSERT INTO artifacts(id,project_id,run_id,kind,name,uri,content_hash,metadata,created_at) VALUES(?,?,?,?,?,?,?,?,?)`).run(id,projectId,runId??null,kind,name,uri??null,contentHash??null,serialized,now);db.prepare(`INSERT INTO artifact_revisions(id,artifact_id,revision,content_hash,uri,metadata,source_kind,source_ref,created_at) VALUES(?,?,?,?,?,?,?,?,?)`).run(`REV-${id}-1`,id,1,contentHash??null,uri??null,serialized,kind,runId??null,now);});tx();return id;}
export function appendArtifactRevision(artifactId:string,sourceKind:string,metadata:unknown,contentHash?:string|null,uri?:string|null,sourceRef?:string|null){if(!getArtifact(artifactId))throw new Error("artifact not found");const row=db.prepare(`SELECT COALESCE(MAX(revision),0) AS revision FROM artifact_revisions WHERE artifact_id=?`).get(artifactId) as {revision:number};const revision=row.revision+1;db.prepare(`INSERT INTO artifact_revisions(id,artifact_id,revision,content_hash,uri,metadata,source_kind,source_ref,created_at) VALUES(?,?,?,?,?,?,?,?,?)`).run(`REV-${artifactId}-${revision}`,artifactId,revision,contentHash??null,uri??null,JSON.stringify(metadata??{}),sourceKind,sourceRef??null,new Date().toISOString());return revision;}
export function getArtifact(id:string){return db.prepare(`SELECT * FROM artifacts WHERE id=?`).get(id);}
export function listArtifacts(projectId:string){return db.prepare(`SELECT * FROM artifacts WHERE project_id=? ORDER BY created_at ASC`).all(projectId);}
export function listArtifactRevisions(artifactId:string){return db.prepare(`SELECT * FROM artifact_revisions WHERE artifact_id=? ORDER BY revision ASC`).all(artifactId);}
export function linkArtifacts(parentArtifactId:string,childArtifactId:string,relation:string){if(!getArtifact(parentArtifactId))throw new Error("parent artifact not found");if(!getArtifact(childArtifactId))throw new Error("child artifact not found");if(parentArtifactId===childArtifactId)throw new Error("artifact cannot link to itself");db.prepare(`INSERT OR IGNORE INTO artifact_links(parent_artifact_id,child_artifact_id,relation,created_at) VALUES(?,?,?,?)`).run(parentArtifactId,childArtifactId,relation,new Date().toISOString());}
export function listArtifactLinks(projectId:string){return db.prepare(`SELECT l.parent_artifact_id,l.child_artifact_id,l.relation,l.created_at FROM artifact_links l JOIN artifacts p ON p.id=l.parent_artifact_id JOIN artifacts c ON c.id=l.child_artifact_id WHERE p.project_id=? AND c.project_id=? ORDER BY l.created_at ASC`).all(projectId,projectId);}
export function listArtifactChildren(parentArtifactId:string){return db.prepare(`SELECT * FROM artifacts WHERE parent_artifact_id=? ORDER BY created_at ASC`).all(parentArtifactId);}
export function requestApproval(projectId:string,action:string,payload:unknown){const id=randomUUID();db.prepare(`INSERT INTO approvals(id,project_id,action,payload,created_at) VALUES(?,?,?,?,?)`).run(id,projectId,action,JSON.stringify(payload),new Date().toISOString());return id;}
export function listApprovals(projectId?:string){return projectId?db.prepare(`SELECT * FROM approvals WHERE project_id=? ORDER BY created_at DESC`).all(projectId):db.prepare(`SELECT * FROM approvals ORDER BY created_at DESC`).all();}
